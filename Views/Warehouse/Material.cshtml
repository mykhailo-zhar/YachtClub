@{
    var MethodName = typeof(Material).Name;
    string Create = CRUD.Create + MethodName;
    string Edit = CRUD.Edit + MethodName;
    string Delete = CRUD.Delete + MethodName;
}
@model MaterialViewModel

<table class="table table-sm table-striped table-bordered text-white">
    <thead>
        <tr class="table-dark">
            <th class="d-print-table-cell">
                <div class="text-center" style="font-size: 3em">
                    Материалы
                </div>
                <div align="center">
                    <a asp-action="@Create" class="btn btn-primary w-75">
                        Создать
                    </a>
                </div>
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var obj in Model.Materials)
        {
            <tr class="text-black">
                <td>
                    <div class="card" style="width: auto;">
                        <div class="card-body">
                            <div class="position-absolute" style="right:20px;">
                                <a class="btn btn-info" asp-action="@Edit" asp-route-id="@obj.Id"> Изменить </a>
                            </div>
                            <h5 class="card-title">
                                @obj.Name
                            </h5>
                            <h6 class="card-subtitle"> @obj.Type.Name</h6>
                            <div class="card-text">
                                Доступно материалов:  @Model.Availableresources.First(p => p.Material == obj.Id).Format
                            </div>
                        </div>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>

